{
  "version": "2.0.0",
  "configurations": [
    {
      "name": "PowerDMX (Build and Debug)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/bin/PowerDMX",
      "MIMode": "gdb",
      "miDebuggerArgs": "--nx",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "cwd": "${workspaceFolder}/bin",
      "preLaunchTask": "PowerDMX_BuildTask"
    }
  ]
}